# Node / Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# CDKTF / Terraform
.terraform/
.terraform-cache/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
*.tfvars
*.tfvars.json
cdktf.out/
.gen/

# Backend runtime data
/apps/backend/data/*.json
!/apps/backend/data/*.json.example
/apps/backend/data/logs/
/apps/backend/data/nginx/conf.d/*
!/apps/backend/data/nginx/conf.d/default.conf
# Generated Caddy site blocks, one per exposed deployment — runtime state, not source.
/apps/backend/data/caddy/apps/*
!/apps/backend/data/caddy/apps/.gitkeep
/apps/backend/data/.gitea-admin-password
/apps/backend/data/.gitea-admin-token
/apps/backend/data/.headscale-api-key

# Anything running bootstrap() from the repo root (a probe script, a mis-cd'd command) writes the
# same runtime data here instead of apps/backend/data — including a live Headscale API key. Every
# rule above is anchored to /apps/backend/data/, so without this the stray copy is untracked but
# NOT ignored, and one `git add -A` from the root publishes it to a public repo.
/data/

# k3d node-local storage (bind-mounted host cache for local-path-provisioner PVCs)
/.k3d-storage/

# Shared model weight caches (bind-mounted into k3d nodes — see InfrastructureService.ts)
/.vllm-model-cache/
/.tabbyapi-model-cache/

# Staging area for scripts/backup-to-drive.sh — deleted on every run, never meant to persist
/.backup-tmp/

# Cached base cloud image for tests/remote-host-integration.ts's disposable QEMU VM (~600MB,
# re-downloaded on demand if missing — see tests/lib/disposable-vm.ts)
/tests/.vm-cache/

# rclone remotes/passwords (contains the crypt password if not using the OS keyring)
/rclone.conf
# Generated by apps/backend/src/scripts/generate-rclone-config.ts — contains a live Google Drive
# refresh token in plaintext, regenerated fresh on every backup run.
/.rclone-runtime.conf

# Local config & secrets
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS Metadata
# Backups
/backups/

.DS_Store
Thumbs.db
apps/frontend/dist/
scripts/root-node/.root-node-key*
