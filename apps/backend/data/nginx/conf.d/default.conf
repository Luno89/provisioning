server {
    listen 80 default_server;
    server_name _;

    location / {
        default_type text/plain;
        add_header X-Received-Host "$http_host";
        add_header X-Received-Server-Name "$server_name";
        return 200 "Multi-Cloud Provisioning Platform Nginx Proxy Active\n";
    }
}
